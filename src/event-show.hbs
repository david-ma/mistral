<p><a href="/list-events">&larr; All events</a></p>
<h1>{{event.name}}</h1>
{{#if event.description}}<p>{{event.description}}</p>{{/if}}
<p><strong>Grid:</strong> {{event.gridSize}}×{{event.gridSize}}</p>
<p><a href="{{joinUrl}}" class="btn btn-primary">Join and get my card</a></p>
<p class="text-muted">You’ll get a card with randomised prompts. Click each prompt to take or upload a photo; we’ll analyse it and add it to your card.</p>
