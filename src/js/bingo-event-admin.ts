console.log("Loading bingo-event-admin.ts");

// This is for /usr/local/dev/Thalia/websites/smugmug/src/edit-event.hbs

// We should use D3.js to draw the bingo cards in #view-all-cards #bingo-card-root
// We should list all the prompts, and then show a table of the prompts, and the photos that have been uploaded for each prompt, with the description of the photo.
// We will also add a score of how well the description matches the prompt, but this is future work. Just write "TODO, use magic AI wand" in this cell.